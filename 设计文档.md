# 早安文案 — 设计文档

> 每日自动生成一条早安鸡汤文案，含公历农历日期、节气节日、7 天主题轮换，通过 Humanizer-zh 去 AI 味后存入 Obsidian。

---

## 时间逻辑

```
每晚 23:00 生成明日文案
  → 23:30 macOS 弹窗推送明日文案（预览）
  → 同时写入 Obsidian

示例：6月8日 23:30 → 推送 6月9日 早安文案
```

### 推送

每晚 23:30，双通道同时推：

| 通道 | 实现 |
|------|------|
| macOS 通知 | `osascript` 弹窗，点击打开 Obsidian |
| Telegram | Bot API `sendMessage` 发到你的聊天框 |

消息格式：

```
🌅 6月9日 早安
丙午马年 四月二十四 · 爱情

"爱不是寻找完美的人，而是学会用不完美的眼光..."

📖 打开 Obsidian 看全文
```

## 输出格式

`项目/早安文案/YYYY-MM-DD.md`，模板：

```markdown
---
date: 2026-06-09
lunar: 丙午马年 四月二十三
weekday: 二
theme: 亲情
solar_term: 芒种
festival: 
---

🌅 早安。今天芒种...

<!-- script
芒种时节，麦浪翻金。家人闲坐，灯火可亲。...
-->
```

---

## 七日主题

| 星期 | 主题 | 方向 |
|------|------|------|
| 一 | 爱情 | 浪漫、亲密关系、心动 |
| 二 | 亲情 | 家人、陪伴、孝道 |
| 三 | 友情 | 知己、信任、缘分 |
| 四 | 心态 | 情绪、压力、自我和解 |
| 五 | 成功学 | 自律、目标、行动力 |
| 六 | 人生哲学 | 意义、选择、放下 |
| 日 | 搞钱思维 | 认知、机会、财富观 |

**节气/节日覆盖**：当日若为重要节气或节日，主题自动切换到节气/节日，忽略星期。

---

## 技术架构

```
launchd (每天 23:00 触发)
  → main.py（目标日期 = 明天）
    → ① calendar_utils.py 查询明天的公历/农历/节气/节日/星期
    → ② prompt.py 构建帶主题的 system prompt
    → ③ DeepSeek API 生成初稿文案
    → ④ dedup.py 查 SQLite → 重复则重新生成（最多 3 次）
    → ⑤ prompt.py Humanizer-zh 规则做第二轮 prompt → DeepSeek 去 AI 味
    → ⑥ write_markdown() 写入 Obsidian
    → ⑦ push.py 双通道推送（macOS 通知 + Telegram Bot）
    → ⑦ macOS 通知（成功/失败）
```

### 各组件

| 文件 | 职责 |
|------|------|
| `main.py` | 入口，串联全流程。`--reroll` 重跑今日 |
| `calendar_utils.py` | 公历→农历转换、二十四节气判断、节日匹配 |
| `prompt.py` | 文案生成 prompt 构建 + Humanizer-zh 去 AI 味 prompt |
| `dedup.py` | SQLite 存储历史文案 SHA256，查重 |
| `push.py` | macOS 通知 + Telegram Bot 推送 |
| `config.py` | API Key、路径、节日列表、Telegram token 等配置 |
| `com.jason.morning.plist` | launchd 配置，每天 23:00 触发 |

### 依赖

```
zhdate        # 农历 + 节气
requests      # DeepSeek API 调用
```

### Humanizer-zh 集成

不调用 Claude Code skill，而是提取其核心的 24 条 AI 痕迹检测规则，构建为 DeepSeek system prompt，让 DeepSeek 自行完成去 AI 味重写。

---

## 去重策略

```
SHA256(文案全文) → 查 SQLite history 表
  ├─ 命中 → 加随机因子重新生成（最多 3 次）
  └─ 未命中 → 通过，写入 history
```

节气锚点天然提供主题多样性，大幅降低碰撞概率。

---

## 容错

| 场景 | 处理 |
|------|------|
| API 不可用 | macOS 弹窗通知 "早安文案生成失败" |
| 重试 3 次仍重复 | 降级：跳过今日，记录日志 |
| launchd 漏跑（睡眠中） | launchd 自动在唤醒后补跑，但推送已过时效 → 仅写入 Obsidian，不推通知 |
| 手动不满意 | `python main.py --reroll` 覆盖明日文案 |

---

## 项目状态

- ✅ **2026-06-09 完成**：核心功能（历法、生成、去重、macOS 推送、launchd 定时）
- ⏳ **待配置**：Telegram Bot（Token 已创建，需设环境变量启用）
- 📁 **文件清单**：`main.py`, `config.py`, `calendar_utils.py`, `prompt.py`, `dedup.py`, `push.py`
- ⏰ **定时**：launchd `com.jason.morning`，每天 23:00 触发
- 📋 **测试通过**：`--reroll` 重跑、去重、macOS 通知均正常

---

## 未来规划

- [ ] **二期**：WebUI 前端（查看历史、预览今日、手动重跑）
- [ ] **三期**：一键导出 MoneyPrinterTurbo 格式，自动生成早安视频
- [ ] 推送通知（微信）—— 等企业微信
- [ ] 文案风格偏好学习
